fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
numpy>=1.26.0
scikit-learn>=1.5.0
requests>=2.32.0
pytest>=8.2.0
httpx>=0.27.0
python-dotenv>=1.0.0
# seed_firestore.py imports this inside a try/except ImportError and falls back to a dry-run without
# it, so a valid service-account key could be supplied and nothing written while the run still
# looked like a successful validation. Pinned here so `python seed_firestore.py <key>` seeds.
firebase-admin>=6.5.0
