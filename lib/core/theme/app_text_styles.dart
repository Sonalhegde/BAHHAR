import 'package:flutter/material.dart';

/// Typography definitions supporting both Latin and Arabic typefaces (e.g. Cairo)
class AppTextStyles {
  // TODO: Define shared TextThemes with Cairo / Noto Sans Arabic font fallbacks
}
