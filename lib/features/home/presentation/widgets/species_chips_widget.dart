import 'package:flutter/material.dart';

/// Species filter chips widget
class SpeciesChipsWidget extends StatelessWidget {
  const SpeciesChipsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Implement horizontal species selector chips (Kingfish, Tuna, Hammour, etc.)
    return const Placeholder();
  }
}
