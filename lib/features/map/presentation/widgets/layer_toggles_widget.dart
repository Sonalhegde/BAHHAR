import 'package:flutter/material.dart';

/// Map layer toggles widget (Sea Surface Temp, Bathymetry, Currents)
class LayerTogglesWidget extends StatelessWidget {
  const LayerTogglesWidget({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Implement floating layer toggle bottom sheet / buttons
    return const Placeholder();
  }
}
