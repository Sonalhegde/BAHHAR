import 'package:flutter/material.dart';

/// Map species filter chips widget
class MapSpeciesFilterChips extends StatelessWidget {
  const MapSpeciesFilterChips({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Implement species filtering for hotspot map markers
    return const Placeholder();
  }
}
