import 'package:flutter_test/flutter_test.dart';
import 'package:bahhar/app.dart';

void main() {
  testWidgets('BahharApp smoke test', (WidgetTester tester) async {
    // TODO: Add widget smoke tests once UI components are wired
    expect(true, isTrue);
  });
}
