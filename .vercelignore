# Static landing site on Vercel: upload ONLY the website/ folder.
# The project's Root Directory is `website`, so Vercel serves website/* at the site root
# and reads its deploy config from website/vercel.json.

# Deny everything by default ...
*

# ... then allow the static site folder and its contents (nothing else).
!website
!website/**
